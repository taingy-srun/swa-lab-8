server:
  port: 8762

spring:
  application:
    name: EurekaServiceTwo

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      default-zone: http://127.0.0.1:8761/eureka/,http://127.0.0.1:8762/eureka/
#  instance:
#    hostname: server2
